---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: instances.io.loopholelabs.architekt
spec:
  group: io.loopholelabs.architekt
  names:
    kind: Instance
    listKind: InstanceList
    plural: instances
    singular: instance
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Instance is the Schema for the instances API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InstanceSpec defines the desired state of Instance
            properties:
              nodeName:
                description: NodeName is the name of the node that the instance should
                  run on
                type: string
              packageRaddr:
                description: PackageAddr defines the original remote package address
                type: string
            type: object
          status:
            description: InstanceStatus defines the observed state of Instance
            properties:
              nodeName:
                description: NodeName is the name of the node that the instance is
                  currently running on
                type: string
              packageRaddr:
                description: PackageAddr defines the current remote package address
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
